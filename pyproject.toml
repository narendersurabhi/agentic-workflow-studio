[tool.ruff]
line-length = 100
extend-exclude = ["services/api/app/alembic/versions"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]

[tool.ruff.format]
quote-style = "double"

[tool.mypy]
python_version = "3.11"
strict = true

[tool.pytest.ini_options]
addopts = "-q --ignore=workspace/repos"
testpaths = [
  "libs/core/tests",
  "services/api/tests",
  "services/critic/tests",
  "services/planner/tests",
  "services/policy/tests",
  "services/worker/tests",
  "tests",
]
